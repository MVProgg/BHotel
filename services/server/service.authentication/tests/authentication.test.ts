import { describe, test } from '@jest/globals'
import { Token } from '@bhotel/interfaces'

describe('token checking', () => {
    test('access token generation', () => {
        const token: Token = new Token()
    })
})